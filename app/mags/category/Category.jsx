import React from 'react';
import {redirect} from "next/dist/server/api-utils";

const Category = () => {
    // redirect('/not-found')
    return (
        <div>
            hi
        </div>
    );
};

export default Category;